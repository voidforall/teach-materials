# Notes

- Mission gathered 2026-08-23: user works inside agent harnesses (ZCode, Claude Code, custom skills) and wants to engineer, not just use, the layer around the model. Requested bilingual EN/中文 and "complete all lessons without stopping" — full course authored in one session.
- Course style follows the Apache Arrow course: shared `assets/lesson.css`, `.keystone` for the core aha, `.antipattern`, details-based retrieval quizzes, `.task` for hands-on work, `.source` primary-source block, `.nav` footer with next-lesson link, `.langbtn` EN↔中 toggle.
- Accent color: violet (#6b3fa0), distinct from Arrow teal and RabbitMQ amber.
- Lessons are loop-first: show the actual prompt/tool/loop artifact, then the principle.
